<Window x:Class="WpfReportCreator.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfReportCreator.View"
        mc:Ignorable="d" Style="{StaticResource win}"
        Title="MainWindow" Height="350" Width="525" WindowState="Maximized"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Border Grid.Row="0" Margin="3" Padding="3" CornerRadius="3" BorderThickness="2" BorderBrush="Orange">
            <StackPanel >
                <Button x:Name="btnProductView" Content="Product View"  Style="{StaticResource btnMenu}"
                    Click="btnProductView_Click"/>
                <Button x:Name="btnSampleView" Content="Sample View"   Style="{StaticResource btnMenu}"
                    Click="btnSampleView_Click"/>
                <Button x:Name="LogIn" Content="Log To Edit" Style="{StaticResource btnMenu}"/>
            </StackPanel>
        </Border>

        <ContentControl x:Name="mainContent" Grid.Column="1" Margin="3"/>
    </Grid>
</Window>
