<Window x:Class="WpfReportCreator.View.TargetEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfReportCreator.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="TargetEdit" Height="300" Width="300" Style="{StaticResource win}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="Choose VHP Record" Style="{StaticResource btnNormal}"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Give Up" Style="{StaticResource btnNormal}"/>
            <Button Content="Save" Style="{StaticResource btnNormal}"/>
        </StackPanel>
    </Grid>
</Window>
