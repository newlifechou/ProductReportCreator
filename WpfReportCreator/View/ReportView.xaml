<UserControl x:Class="WpfReportCreator.View.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfReportCreator.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.ColumnSpan="2"></Grid>
        <ListBox Grid.Row="1" Grid.Column="0"/>
        <ListBox Grid.Row="1" Grid.Column="1"/>
        <Button Grid.Row="2" Grid.Column="0" Content="Create Product Report" Style="{StaticResource btnNormal}"/>
        <Button Grid.Row="2" Grid.Column="1" Content="Create COA Report" Style="{StaticResource btnNormal}"/>
    </Grid>
</UserControl>
