<Window x:Class="WpfReportCreator.View.LogInView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfReportCreator.View"
        mc:Ignorable="d"
        Title="LogIn" Height="150" Width="300" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="Log In When you want to edit the data"
                   Grid.Row="0" Grid.ColumnSpan="2" Foreground="Red"/>
        <TextBlock Text="Pass Code" Grid.Row="1" Grid.Column="0"/>
        <TextBox x:Name="txtPassCode" Text="" Grid.Row="1" Grid.Column="1" Style="{StaticResource txtNormal}"/>
        <Button Content="Log In" Grid.Row="2" Grid.ColumnSpan="2" Style="{StaticResource btnNormal}" Click="LogIn_Click"/>
    </Grid>
</Window>
